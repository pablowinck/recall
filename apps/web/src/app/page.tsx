import { RecallWorkspace } from '@/features/workspace/recall-workspace';

/** Abre o espaço de estudo. Exemplo: GET /. */
export default function RecallPage(): React.JSX.Element {
  return <RecallWorkspace />;
}
